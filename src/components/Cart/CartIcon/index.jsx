
function Index(icon) {
    return (
        <div className={'rounded-full size-[68px] p-[19px] bg-primary'}>
            <i className={`${icon} text-16 text-white text-center`}></i>
        </div>
    );
}

export default Index;